import React, { useContext } from 'react';
import { Context } from "../store/appContext";

function EditCancha(cancha) {
    const { actions, store } = useContext(Context)



    return (
        <div>{ }</div>
    )
}

export default EditCancha